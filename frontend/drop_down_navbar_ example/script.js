// JavaScript is optional for this basic dropdown. 
// However, if you want to add more dynamic behavior, you can do so here.

// Example: Close the dropdown if the user clicks outside of it.
/*
window.onclick = function(event) {
    if (!event.target.matches('.dropbtn')) {
        var dropdowns = document.getElementsByClassName("dropdown-content");
        for (var i = 0; i < dropdowns.length; i++) {
            var openDropdown = dropdowns[i];
            if (openDropdown.style.display === 'block') {
                openDropdown.style.display = 'none';
            }
        }
    }
}

*/

function onJobsClick() {
//	alert('jobs clicked');
	//document.getElementById("jobs").className="";

}
